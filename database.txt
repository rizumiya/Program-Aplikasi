BEGIN TRANSACTION;
CREATE TABLE logins (
            login_id integer primary key autoincrement,
            username text,
            password text,
            status text
        );
INSERT INTO "logins" VALUES(1,'qwe','qwe','on');
CREATE TABLE settings (
            set_id integer primary key autoincrement,
            id_login integer, 
            cameraNo integer, 
            def_subject text, 
            showAnswer integer, 
            autoSave integer
        );
INSERT INTO "settings" VALUES(1,1,1,'Matematika (23/5/23)',1,0);
CREATE TABLE subjects (
            sub_id integer primary key autoincrement,
            sub_name text,
            sub_totalQuestion integer,
            sub_choices integer,
            sub_answer text,
            id_login integer
        );
INSERT INTO "subjects" VALUES(1,'Matematika (23/5/23)',10,4,'0, 1, 2, 3, 3, 2, 1, 0, 1, 2',1);
DELETE FROM "sqlite_sequence";
INSERT INTO "sqlite_sequence" VALUES('logins',1);
INSERT INTO "sqlite_sequence" VALUES('settings',1);
INSERT INTO "sqlite_sequence" VALUES('subjects',1);
COMMIT;
