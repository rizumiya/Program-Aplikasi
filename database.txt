BEGIN TRANSACTION;
CREATE TABLE logins (
            login_id integer primary key autoincrement,
            username text,
            password text,
            status text
        );
INSERT INTO "logins" VALUES(5,'w','w','on');
CREATE TABLE settings (
            set_id integer primary key autoincrement,
            id_login integer, 
            cameraNo integer, 
            def_subject text, 
            showAnswer integer, 
            autoSave integer
        );
INSERT INTO "settings" VALUES(5,5,0,'No Subject',1,1);
CREATE TABLE subjects (
            sub_id integer primary key autoincrement,
            sub_name text,
            sub_totalQuestion integer,
            sub_choices integer,
            sub_answer text,
            id_login integer
        );
DELETE FROM "sqlite_sequence";
INSERT INTO "sqlite_sequence" VALUES('logins',5);
INSERT INTO "sqlite_sequence" VALUES('settings',5);
INSERT INTO "sqlite_sequence" VALUES('subjects',2);
COMMIT;
